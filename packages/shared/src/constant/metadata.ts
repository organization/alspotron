// import rootPackage from '../../../../package.json';

export const metadata = {
  name: 'Alspotron', // rootPackage.name,
  description: 'A new Alspotron', // rootPackage.description,
  version: 'v1.0.0-renew', // rootPackage.version,
};
